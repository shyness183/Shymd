import type { FileNode } from '../types'
import { defaultDoc } from './defaultDoc'

export const sampleFiles: FileNode[] = [
  {
    name: '欢迎使用 Shymd.md',
    type: 'file',
    content: defaultDoc,
  },
]
